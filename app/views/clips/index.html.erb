<% @clips.each do |clip| %>
  <div class="clip row clearfix">
    <h2>
      <%= link_to clip.title, clip %><br>
      <small class="author">Submitted <%= time_ago_in_words(clip.created_at) %> by <%= clip.user.email %></small>
<% end %>